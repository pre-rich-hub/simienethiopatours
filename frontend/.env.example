# Optional. When set, inquiry submissions are forwarded as JSON to this endpoint.
CONTACT_WEBHOOK_URL=
NEXT_PUBLIC_SITE_URL=https://gondarsimientours.com

# Backend API base URL (Express backend). Defaults to http://localhost:5000 if unset.
NEXT_PUBLIC_API_URL=http://localhost:5000
